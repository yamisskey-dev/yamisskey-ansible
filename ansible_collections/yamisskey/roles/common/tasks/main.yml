---
- name: Update and upgrade apt packages
  ansible.builtin.apt:
    update_cache: true
    upgrade: 'dist'
  tags:
    - update

- name: Install essential packages
  ansible.builtin.apt:
    name: '{{ packages }}'
  tags:
    - packages
