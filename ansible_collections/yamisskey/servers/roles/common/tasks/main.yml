---
- name: Install system packages via packages role
  include_role:
    name: yamisskey.servers.packages
  vars:
    packages_profile: "{{ nix_package_profile | default(nix_home_manager_config, true) | default('server', true) }}"
  tags:
    - always

- name: Display package management strategy
  debug:
    msg: |
      ✅ System Package Management via Ansible
      📦 All packages managed via ansible.builtin.package
      
      📋 Common role packages provided by system packages:
      - prometheus, rclone, apparmor, auditd, tor, go
      
      🚀 Ready for infrastructure automation!
  tags:
    - always
