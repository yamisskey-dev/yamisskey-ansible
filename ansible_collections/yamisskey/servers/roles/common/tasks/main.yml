---
- name: Update and upgrade apt packages
  apt:
    update_cache: true
    upgrade: 'dist'
  tags:
    - update

- name: Install essential packages
  apt:
    name: '{{ packages }}'
  tags:
    - packages
