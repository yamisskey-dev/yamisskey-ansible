# misskey settings
# MISSKEY_URL=https://{{ domain }}/

# db settings
POSTGRES_PASSWORD={{ misskey_db_pass | default('example_misskey_pass') }}
# DATABASE_PASSWORD=${POSTGRES_PASSWORD}
POSTGRES_USER={{ misskey_db_user | default('example_misskey_user') }}
# DATABASE_USER=${POSTGRES_USER}
POSTGRES_DB={{ misskey_db_name | default('example_misskey_db') }}
# DATABASE_DB=${POSTGRES_DB}
DATABASE_URL="postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}"
