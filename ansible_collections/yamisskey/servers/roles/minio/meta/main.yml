---
galaxy_info:
  author: yamisskey-dev
  description: Enterprise-grade MinIO object storage deployment and management
  company: yamisskey
  license: MIT
  min_ansible_version: "2.10"
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
    - name: Debian
      versions:
        - bullseye
        - bookworm
  galaxy_tags:
    - storage
    - minio
    - object-storage
    - docker
    - backup
    - s3

dependencies:
  - role: yamisskey.servers.common
  - role: yamisskey.servers.security

collections:
  - community.docker
  - ansible.posix