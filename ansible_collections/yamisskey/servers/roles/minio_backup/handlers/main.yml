---
- name: Reload systemd
  systemd:
    daemon_reload: true
  become: true

- name: Enable minio-backup timer
  systemd:
    name: minio-backup.timer
    enabled: true
    state: started
  become: true
