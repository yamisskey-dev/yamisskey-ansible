---
- name: Display Docker package status
  debug:
    msg: |
      ✅ Docker packages managed via system packages
      📦 Available tools: docker, docker-compose
      🐳 Ready for container operations!

- name: Verify Docker is available
  command: docker --version
  register: docker_version_check
  failed_when: false
  changed_when: false

- name: Display Docker version
  debug:
    msg: "Docker version: {{ docker_version_check.stdout }}"
  when: docker_version_check.rc == 0

- name: Add user to docker group
  ansible.builtin.user:
    name: "{{ ansible_user | default(ansible_user_id | default('root')) }}"
    groups: docker
    append: true
  when: not ansible_check_mode

- name: Enable and start Docker service
  ansible.builtin.systemd:
    name: docker
    enabled: true
    state: started
  when: not ansible_check_mode
  register: docker_service
  failed_when: false
