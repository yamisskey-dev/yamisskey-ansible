---
- name: Converge
  hosts: all
  become: false
  tasks:
    - name: "Include yamisskey.servers.ghq"
      include_role:
        name: yamisskey.servers.ghq
      vars:
        ghq_operation: install
