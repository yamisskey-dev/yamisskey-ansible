[defaults]
roles_path = ../../ansible_collections/yamisskey/servers/roles
host_key_checking = False
timeout = 30
gathering = smart
fact_caching = memory
stdout_callback = ansible.builtin.default
bin_ansible_callbacks = True
remote_tmp = /tmp/ansible
# セキュリティ強化: ログに機密情報を記録しない
no_log = True
# セキュリティ強化: タスクのターゲットホストを制限
any_errors_fatal = True
# セキュリティ強化: ファクト収集の最小化
gather_subset = min

[inventory]
enable_plugins = host_list, script, auto, ini, toml

[ssh_connection]
# セキュリティ強化: SSH接続の暗号化強化
ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o Ciphers=aes256-ctr,aes192-ctr,aes128-ctr
pipelining = True

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False

# セキュリティ強化: デバッグ情報の制限
[persistent_connection]
connect_timeout = 30
connect_retries = 3
