- name: Prepare
  hosts: all
  gather_facts: false
  tasks:
    - name: Ensure /var/tmp/ansible exists with sticky bit
      ansible.builtin.file:
        path: /var/tmp/ansible
        state: directory
        mode: "1777"
